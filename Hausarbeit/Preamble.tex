\documentclass[index=totoc, bibliography=totoc, listof=totoc, paper=a4, fontsize=11pt]{scrartcl}

\input{commands}

\usepackage[english, ngerman]{babel}
\usepackage[backend=biber,style=alphabetic,]{biblatex}
\usepackage{graphicx}
\graphicspath{ {../Results/} }
\usepackage{hyperref}  % for clickable references when citing or referring to labels
\usepackage{enumerate}

\bibliography{Literatur}

\setlength{\parindent}{0pt}
\setlength{\parskip}{4pt}
